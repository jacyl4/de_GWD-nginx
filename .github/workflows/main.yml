name: Auto NGINX

on:
  workflow_dispatch:

env:
    NGINX_VERSION: 1.19.4

jobs:
  build:
    runs-on: buster
    steps:
    - name: Checkout
      uses: actions/checkout@v2
      
    - name: build nginx
      run: |
        chmod +x nginx_amd64.sh
        ./nginx_amd64.sh

